# clasp / Google OAuth credentials - NEVER commit these
.clasprc.json
*.clasprc.json

# Personal config (pushed by clasp, never committed)
config.local.js

# Node
node_modules/
npm-debug.log*

# OS noise
.DS_Store
Thumbs.db
